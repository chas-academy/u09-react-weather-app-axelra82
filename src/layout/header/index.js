/* eslint-disable import/no-anonymous-default-export */
import React from 'react';

import './style.scss';

export default () => {

    return(
        <header>
            <section className='container'>
                
		    </section>
        </header>
    );
}